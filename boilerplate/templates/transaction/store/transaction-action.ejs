import * as actionType from './transaction-action-type';

export const setFormID = (data) => {
    return {
        type: actionType.SET_FORM_ID,
        payload: data,
    };
};

export const updateFormID = (data) => {
    return (dispatch) => {
        dispatch(setFormID(data));
    };
};